This file is the full text of the Poops documentation — every page's Markdown concatenated in
one place so an LLM can ingest the whole corpus in a single pass. For the link index (a shorter
map of the same pages), see [`llms.txt`](llms.txt).

Pages are ordered as they appear on the site; each is preceded by its title and canonical URL.
Code samples are shown as authored — some contain template tags (`{% … %}`, `{{ … }}`) that the
site renders at build time.
